[pytest]
testpaths = tests
pythonpath = tests
markers =
    realdata: runs the real pipeline on the supplied microscopy (slow, needs Cellpose)
addopts = -m "not realdata"
filterwarnings =
    ignore::DeprecationWarning
